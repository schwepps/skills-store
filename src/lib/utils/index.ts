/**
 * Utility functions barrel export
 *
 * Re-exports all utility modules for convenient imports:
 * ```typescript
 * import { focusManagement } from '@/lib/utils';
 * ```
 */

export * from './accessibility';
