# Skills Store Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# GitHub Configuration (for sync operations)
# ============================================

# GitHub Personal Access Token (recommended for higher rate limits)
# Increases API rate limit from 60 to 5000 requests/hour
# Create at: https://github.com/settings/tokens
# Required scopes: repo (read access to public repos)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Application Configuration
# ============================================

# Base URL for meta tags and OG images
# Use http://localhost:3000 for development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ============================================
# Supabase Configuration (REQUIRED)
# ============================================
# The app requires Supabase for data storage.
# Get credentials from: Supabase Dashboard > Settings > API

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co

# Supabase Publishable Key (client-safe)
# New format: sb_publishable_xxx
# Legacy format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Supabase Secret Key (server-side only, keep secret!)
# New format: sb_secret_xxx
# Legacy format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Required for sync operations (writes to database)
SUPABASE_SECRET_KEY=sb_secret_xxx

# ============================================
# Legacy Key Names (still supported)
# ============================================
# If your Supabase project uses older JWT-based keys,
# you can use these legacy variable names instead:
#
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# Sync API Security
# ============================================

# Secret for authorizing manual sync API calls
# Generate a random string: openssl rand -hex 32
# Required in production, optional in development
SYNC_SECRET=your-random-secret-here

# Vercel Cron Secret (automatically set by Vercel)
# Used for authenticating scheduled sync jobs
# Do NOT set this manually - Vercel sets it automatically
# CRON_SECRET=auto-set-by-vercel

# ============================================
# Skill Request Configuration
# ============================================
# Repository where "Request a Skill" form submissions create GitHub issues
# Users who fork this repo should update these to their own repository
SKILL_REQUEST_REPO_OWNER=schwepps
SKILL_REQUEST_REPO_NAME=skills

# ============================================
# GitHub Token Scopes
# ============================================
# The GITHUB_TOKEN above is used for:
# - Repository validation (read access to public repos)
# - Creating skill request issues on SKILL_REQUEST_REPO
#
# For skill request feature, ensure the token has:
# - public_repo scope (for creating issues on SKILL_REQUEST_REPO)
